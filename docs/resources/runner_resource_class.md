---
page_title: "circleci_runner_resource_class Resource - circleci"
subcategory: ""
description: |-
  Manages a CircleCI self-hosted runner resource class.
---

# circleci_runner_resource_class (Resource)

Manages a CircleCI self-hosted runner resource class. Resource classes define a pool of self-hosted runners within an organization namespace.

## Example Usage

```terraform
resource "circleci_runner_resource_class" "example" {
  organization_id = "00000000-0000-0000-0000-000000000000"
  resource_class  = "my-namespace/my-runner"
  description     = "My self-hosted runner"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `resource_class` (String) The resource class name in `namespace/name` format (e.g. `myorg/myrunner`). Changing this value forces a new resource to be created.

### Optional

- `description` (String) Description of the runner resource class.
- `force_delete` (Boolean) If true, deletes the resource class even if it has associated tokens.
- `org_id` (String) The unique identifier (UUID) of the organization that owns this runner resource class.

This is the same field as the deprecated `organization_id`; set exactly one of the two.
- `organization_id` (String, Deprecated) The unique identifier (UUID) of the organization that owns this runner resource class.

~> **Deprecated in favour of `org_id`**, which matches CircleCI's own naming. Both work and mean the same thing; set exactly one. Switching from this attribute to `org_id` does not replace the resource.

### Read-Only

- `id` (String) Unique identifier (UUID) of the runner resource class.

## Import

Import is supported using the resource class name in `namespace/name` format:

```shell
terraform import circleci_runner_resource_class.example "my-namespace/my-runner"
```

After import, Terraform will call the API to populate the full state. Note that `organization_id` is resolved from the resource class during the subsequent read.
