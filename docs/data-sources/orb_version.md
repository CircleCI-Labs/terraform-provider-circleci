---
page_title: "circleci_orb_version Data Source - circleci"
subcategory: ""
description: |-
  Looks up one published version of a CircleCI orb, including its source.
---

# circleci_orb_version (Data Source)

Looks up one published version of a CircleCI orb, by version id, or by orb id plus a version reference.

The version reference may be an exact version such as `1.2.3`, a `dev:<label>` version, or an alias such as `volatile` for whatever the newest version is. When an alias resolves to a different version string, the `version` attribute reports the resolved one.

`orb_id` is required unless `id` is set: the CircleCI API's orb version collection is always scoped to a single orb, so there is no way to search versions globally.

Use this to read the published YAML source, or to resolve the UUID needed to import a `circleci_orb_version`.

## Availability

| | |
| --- | --- |
| **CircleCI Cloud** | Yes |
| **CircleCI Server** | No — served by the CircleCI v3 API, which a Server installation does not route to its public API service. Using this with `deployment = "server"` reports an explicit error at plan time rather than the confusing HTTP 404 the request would otherwise produce. |
| **API** | `GET /api/v3/orb/versions/{id}`, or `GET /api/v3/orb/versions` filtered by orb and version |
| **Organization type** | Any. |
| **Token** | Any valid API token. |

## Example Usage

```terraform
data "circleci_orb" "node" {
  full_name = "circleci/node"
}

# Resolve an exact version.
data "circleci_orb_version" "pinned" {
  orb_id  = data.circleci_orb.node.id
  version = "5.1.0"
}

# Or resolve an alias to whatever the newest version is.
data "circleci_orb_version" "newest" {
  orb_id  = data.circleci_orb.node.id
  version = "volatile"
}

# Or read one directly by id.
data "circleci_orb_version" "by_id" {
  id = "44444444-4444-4444-4444-444444444444"
}

output "published_source" {
  value = data.circleci_orb_version.pinned.source
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Unique identifier (UUID) of the orb version. Set either `id`, or `orb_id` together with `version`.
- `orb_id` (String) Unique identifier (UUID) of the orb the version belongs to. Required unless `id` is set, because the version collection is always scoped to one orb.
- `version` (String) The version reference to resolve, such as `1.2.3`, `dev:alpha` or `volatile`. Required unless `id` is set.

### Read-Only

- `created_at` (String) When the version was published, as an RFC 3339 timestamp.
- `orb_name` (String) Fully qualified name of the orb, `<namespace>/<orb>`.

No orb version route reports this, so it is resolved from the orb itself. Looking a version up by `orb_id` and `version` gets it for free, because the orb has to be read anyway to build the reference the API needs; looking one up by `id` costs one extra request.
- `source` (String) The YAML source CircleCI stored for this version.
