---
page_title: "circleci_deploy_environments Data Source - circleci"
subcategory: ""
description: |-
  Fetches every CircleCI deploy/release environment in an organization.
---

# circleci_deploy_environments (Data Source)

Fetches every CircleCI deploy/release environment in an organization. Environments are declared in a
project's `.circleci/config.yml` (the `environment` key on a deploy job) rather than created through the
API, so this is read-only.

Pagination is followed internally, so the result covers every environment rather than one page.

## Availability

| | |
| --- | --- |
| **CircleCI Cloud** | Yes |
| **CircleCI Server** | No — deploy/release tracking (the API) is not deployed on CircleCI Server; its routes are absent from the Server gateway routes served entirely. Using this data source with `deployment = "server"` reports an explicit error. |
| **API** | `GET /api/v2/deploy/environments` |
| **Organization type** | Any. |
| **Token** | Any valid API token. |

-> **Members are a separate concern.** Use [`circleci_deploy_environment`](deploy_environment) to fetch one
environment by id, e.g. after looking its id up here by name.

## Example Usage

```terraform
# Lists every deploy/release environment in the organization.
# Available on CircleCI Cloud only.
data "circleci_deploy_environments" "all" {
  organization_id = "00000000-0000-0000-0000-000000000000"
}

output "circleci_deploy_environment_names" {
  value = [for env in data.circleci_deploy_environments.all.environments : env.name]
}

# Environments are addressed by id elsewhere (e.g. circleci_deploy_component's
# version list), so look one up by name when needed.
output "circleci_prod_environment_id" {
  value = one([
    for env in data.circleci_deploy_environments.all.environments : env.id
    if env.name == "prod"
  ])
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `org_id` (String) The unique identifier (UUID) of the organization to read deploy environments from.

This is the same field as the deprecated `organization_id`; set exactly one of the two.
- `organization_id` (String, Deprecated) The unique identifier (UUID) of the organization to read deploy environments from.

~> **Deprecated in favour of `org_id`**, which matches CircleCI's own naming. Both work and mean the same thing; set exactly one.

### Read-Only

- `environments` (Attributes List) The environments in the organization, sorted by name. (see [below for nested schema](#nestedatt--environments))

<a id="nestedatt--environments"></a>
### Nested Schema for `environments`

Read-Only:

- `created_at` (String) When the environment was first observed, as an RFC 3339 timestamp.
- `description` (String) Description of the environment.
- `id` (String) Unique identifier (UUID) of the environment.
- `labels` (Map of String) Labels attached to the environment, as a map of label key to value.
- `name` (String) Name of the environment, e.g. `staging` or `prod`.
- `updated_at` (String) When the environment was last updated, as an RFC 3339 timestamp.
