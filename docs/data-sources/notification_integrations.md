---
page_title: "circleci_notification_integrations Data Source - circleci"
subcategory: ""
description: |-
  Lists CircleCI notification integrations.
---

# circleci_notification_integrations (Data Source)

Lists CircleCI notification integrations (Slack workspace installations, today).

## Availability

| CircleCI Cloud | CircleCI Server |
|---|---|
| yes | **no** |

Integrations are served by the CircleCI v3 API, which CircleCI Server does not route to its public API service.

## There is no resource to create an integration

A Slack workspace is installed through an OAuth flow in the CircleCI web UI, not through this API, so there is deliberately no `circleci_notification_integration` resource. Use this data source to discover the `id` of an integration installed that way, then manage whether it is active or disabled with `circleci_notification_integration_status`.

## Example Usage

```terraform
# List every notification integration (Slack workspace installation, today)
# for one organization.
data "circleci_notification_integrations" "org" {
  org_id = "00000000-0000-0000-0000-000000000000"
}

# List every integration for every organization the calling user belongs to.
data "circleci_notification_integrations" "mine" {}

output "slack_integration_ids" {
  value = [for i in data.circleci_notification_integrations.org.integrations : i.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `org_id` (String) Only return integrations belonging to this organization (UUID). Leave unset to list every organization the calling user belongs to.
- `type` (String) Only return integrations of this type. The only type today is `slack`.

### Read-Only

- `integrations` (Attributes List) The matching integrations. (see [below for nested schema](#nestedatt--integrations))

<a id="nestedatt--integrations"></a>
### Nested Schema for `integrations`

Read-Only:

- `created_at` (String) When the integration was installed, as a UTC timestamp.
- `id` (String) Unique identifier (UUID) of the integration.
- `org_id` (String) Unique identifier (UUID) of the owning organization.
- `org_name` (String) Display name of the owning organization.
- `status` (String) The integration's current status: `active`, `disabled` or `disconnected`.
- `team_id` (String) The Slack team id of the installed workspace.
- `type` (String) The integration type.
- `updated_at` (String) When the integration was last changed, as a UTC timestamp.
- `workspace_name` (String) The Slack workspace's display name.
