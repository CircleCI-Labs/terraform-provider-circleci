---
page_title: "circleci_deploy_environment Data Source - circleci"
subcategory: ""
description: |-
  Fetches one CircleCI deploy/release environment by id.
---

# circleci_deploy_environment (Data Source)

Fetches one CircleCI deploy/release environment by id. Use
[`circleci_deploy_environments`](deploy_environments) to list every environment in an organization, e.g.
to look one up by name — the API has no lookup-by-name route.

## Availability

| | |
| --- | --- |
| **CircleCI Cloud** | Yes |
| **CircleCI Server** | No — deploy/release tracking is not deployed on CircleCI Server. Using this data source with `deployment = "server"` reports an explicit error. |
| **API** | `GET /api/v2/deploy/environments/{id}` |
| **Organization type** | Any. |
| **Token** | Any valid API token. |

## Example Usage

{{ tffile "examples/data-sources/circleci_deploy_environment/data-source.tf" }}

{{ .SchemaMarkdown | trimspace }}
